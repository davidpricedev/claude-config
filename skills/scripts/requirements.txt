# Requirements for skills/scripts Python code
# Generated by examining all imports across Python files in this repository.
#
# Standard library modules (argparse, ast, abc, collections.abc, dataclasses,
# datetime, enum, fcntl, fnmatch, importlib, inspect, itertools, json, os,
# pathlib, pkgutil, random, re, shlex, string, subprocess, sys, tempfile,
# typing) are excluded as they ship with Python.
#
# Internal project modules (skills.*, relative imports, conftest, test_generation)
# are excluded as they are part of this repository.

# Testing framework
pytest

# Property-based testing library (used in tests/test_domain_types.py)
hypothesis
